# Dependencies
node_modules/

# Build output
out/
dist/
release/
*.tsbuildinfo

# Native helper build output (the Windows WASAPI capture binary; source is committed)
native/win-audio-capture/build/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Local env / secrets (none are committed; the API key lives in the OS keychain)
.env
.env.*
